name: beat_saber_song_toolkit
description: Cross-platform Beat Saber song downloader core, starting with BeatSaver support.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.4.0

dependencies:
  archive: ^4.0.7
  crypto: ^3.0.7
  path: ^1.9.1

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.0
