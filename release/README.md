# Release Artifacts

This repository does not track local Windows release binaries.

Use the workflow at `.github/workflows/windows-release.yml` to build the
Windows x64 package on GitHub and download it from the workflow artifacts.
